<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>사계</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" >
    <link rel= "stylesheet" href="/css/MainBoard.css">
    <link href="/css/headers.css" rel="stylesheet">
    <link href="/css/ui.css" rel="stylesheet">
</head>
<body>

{{>Nav/Nav}}
<script src="/JS/FinalLogin.js"></script>
<script src="/JS/RegisterModal.js"></script>
<!-- Wrapper -->
<div id="wrapper">
    <!-- Header -->
    <header id="header">
        <span class="avatar"><img src="/Image/booktop2.png" alt="" /></span>
        <h1>당신과 함께 <strong>새로운 계절</strong>을 나눌 수 있는 <a href="Home">사계</a><br><br>
            <strong>계절의 경험을 나누다.</strong></h1>
    </header>
</div>
<main>
<div class="album py-5 bg-light">
    <div class="container">

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        {{#List}}
            <div class="col">
                <div class="card shadow-sm">
                        <img src="{{file_path1}}"  width="100%" height="225">
                    <div class="card-body">
                        <p class="card-text">{{title}}</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="btn-group">
                                <button type="button" class="btn btn-sm btn-outline-secondary" onclick="location.href='/MainBoardView/{{idx}}'">View</button>
                                {{#Manager}}
                                <button type="button" class="btn btn-sm btn-outline-secondary" >Edit</button>
                                {{/Manager}}
                            </div>
                            <small class="text-muted">{{write_time}}</small>
                        </div>
                    </div>
                </div>
                <hr>
            </div>
        {{/List}}
        </div>
    </div>
{{#Manager}}
    <div id="write">
        <a href="/MainWrite" class="btn btn-sm btn-outline-secondary" >글쓰기</a>
    </div>
{{/Manager}}
    <div id=page style="text-align : center">
        <td colspan="4" >
            {{#page}}
                <a href="?page={{prePage}}" style="margin-right : 1vw;">이전</a>
                <a href="?page={{page}}" style="margin-right : 1vw;">{{nowPage}}</a>
                <a href="?page={{nextPage}}" style="margin-right : 1vw;">다음</a>
            {{/page}}
            <hr>
        </td>
    </div>
</div>

</main>

    <!-- Footer -->
    <footer id="footer">
        <p>&copy; 2022 Graduation Work. Design: <a href="https://www.instagram.com/98p_jm/">JeongMin Park.</a> ::Instragram </p>
    </footer>
<script src="JS/BoardSearch.js"></script>
</body>
</html>